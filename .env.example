# HTTP 服务
HOST=127.0.0.1
PORT=3000

# 请在生产环境使用至少 32 字节的高强度随机值；留空时每次启动随机生成，重启后现有 token 会失效
AUTH_SECRET=
# DATA_DIR=.data

# OpenAI 兼容的 API；可填写 API 根路径或完整的 /chat/completions 地址
# 示例：AI_API_URL=https://api.openai.com/v1
AI_API_URL=
AI_API_KEY=
AI_MODEL=
